name: cardinal
# The workflow replaces these literal tokens before invoking nfpm.
# nfpm does not expand environment variables in YAML values.
arch: "__ARCH__"
platform: linux
version: "__VERSION__"
section: utils
priority: optional
maintainer: kuranix <igorerantaev66@gmail.com>
description: Lightweight container runtime for Linux
vendor: kuranix
homepage: https://github.com/kuranix/cardinal
license: MIT

contents:
  - src: ./cardinal-linux-__BIN_ARCH__
    dst: /usr/bin/cardinal

rpm:
  group: System Environment/Base
  compression: lzma

archlinux:
  pkgbase: cardinal
  packager: kuranix

overrides:
  deb:
    scripts:
      postinstall: packaging/deb/DEBIAN/postinst
